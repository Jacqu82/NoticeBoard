{% extends "::base.html.twig" %}

{% block title %}BoardBundle:Announcement:edit_photo{% endblock %}
{% block body %}
    <div class="container">
    <h2>Aktualne zdjęcie:</h2>
    <img src='{{ asset( photo.getWebPath ) }}' width="350" height="300">
    <h3>Zmień zdjęcie:</h3>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <hr/>
    <a href="{{ path('show_details', { 'id': photo.id }) }}">Powrót do ogłoszenia</a><br/><br/>
    <a href="http://localhost:8000/">Powrót do strony głównej</a>
    </div>
{% endblock %}